

###################################################
## examples/cosmo_zoom_gravity_only_3d/Config.sh ##
###################################################


#--------------------------------------- Gravity treatment
selfgravity: ON                               # gravitational intraction between simulation particles/cells
hierarchical_gravity: ON                      # use hierarchical splitting of the time integration of the gravity
allow_direct_summation: ON                    # Performed direct summation instead of tree-based gravity if number of active particles < DIRECT_SUMMATION_THRESHOLD (= 3000 unless specified differently here)
direct_summation_threshold: 1024          # overrides maximum number of active particles for which direct summation is performed instead of tree based calculation
evalpotential: ON                             # computes gravitational potential


#--------------------------------------- TreePM Options
pmgrid: 256                               # enables particle mesh; number of cells used for grid in each dimension
rcut: 5.5                                 # this can be used to override the maximum radius in which the short-range tree-force is evaluated (in case the treepm algorithm is used). the default value is 4.5, given in mesh-cells.
placehighresregion: 2                     # places a second, high-resolution pm grid; number encodes high-res particle types from which the extent of this second pm grid is calculated.
enlargeregion: 1.1                        # factor by which high res region is increased with respect to max-min position of high-res particles
gridboost: 2                              # factor by which pmgrid is increased in non-periodic (or high res) pm calculation (if not set, code uses 2)
pm_zoom_optimized: ON                         # Particle-mesh calculation that is optimized for cosmological zoom simulations; disable for cosmological volume simulations.


#--------------------------------------- Gravity softening
multiple_node_softening: ON                   # If a tree node is to be used which is softened, this is done with the softenings of its different mass components
individual_gravity_softening: 4+8+16+32   # bitmask with particle types where the softenig type should be chosen with that of parttype 1 as a reference type


#--------------------------------------- Time integration options
tree_based_timesteps: ON                      # non-local timestep criterion (take 'signal speed' into account)


#--------------------------------------- Single/Double Precision
doubleprecision: 1                        # mode of double precision: not defined: single; 1: full double precision 2: mixed, 3: mixed, fewer single precisions; unless short of memory, use 1.
doubleprecision_fftw: ON                      # FFTW calculation in double precision


#--------------------------------------- On the fly FOF groupfinder
fof: ON                                       # enable FoF output
fof_primary_link_types: 2                 # 2^type for the primary dark matter type
fof_secondary_link_types: 1+16+32         # 2^type for the types linked to nearest primaries


#--------------------------------------- Subfind
subfind: ON                                   # enables substructure finder


#--------------------------------------- output fields in snapshots--Default output filds are: position, velocity, ID, mass, spec. internal energy (gas), density(gas)
outputpotential: ON                           # output potential at particle position


#--------------------------------------- output options
process_times_of_outputlist: ON               # goes through times of output list prior to starting the simulaiton to ensure that outputs are written as close to the desired time as possible (as opposed to at next possible time if this flag is not active)
have_hdf5: ON                                 # needed when HDF5 I/O support is desired (recommended)


#--------------------------------------- Testing and Debugging options
debug: ON                                     # enables core-dumps
